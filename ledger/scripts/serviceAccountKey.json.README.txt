Your original serviceAccountKey.json was removed from this download.

That file contained a REAL, LIVE Firebase Admin private key for project
"business-59424". It was already gitignored (never going to source
control) but it had been sitting in the zip you uploaded, which means
it's been exposed outside your machine.

Before doing anything else:
1. Go to Firebase Console -> Project settings -> Service accounts.
2. Revoke/delete that key and generate a new one.
3. Save the new JSON as scripts/serviceAccountKey.json locally (it's
   already in .gitignore, so it will stay out of git) OR, better, set
   FIREBASE_PROJECT_ID / FIREBASE_CLIENT_EMAIL / FIREBASE_PRIVATE_KEY as
   env vars in your deployment platform instead, per .env.local.example.
